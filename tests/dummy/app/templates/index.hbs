{{docs-header}}

{{docs-hero
  logo='ember'
  slimHeading='good'
  strongHeading='point'
  byline='SVG arrows with contextual components.'}}

{{#docs-demo as |demo|}}
  {{#demo.example name='docs-demo-basic.hbs'}}
    {{#goodpoint-arrow as |arrow|}}
      <div class="flex">
      {{#arrow.from}}
        <div class="card">Draw an arrow from here</div>
      {{/arrow.from}}
      {{#arrow.to}}
        <div class="card">To here</div>
      {{/arrow.to}}
      </div>
    {{/goodpoint-arrow}}
  {{/demo.example}}
{{/docs-demo}}